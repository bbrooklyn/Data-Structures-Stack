from stack import Stack

block = Stack(10) # Allocate Memory

block.push('Hello World!') # Push the data to the stack.

block.pop() # Remove the last item added to the stack.

#block.peek() # Returns the last item added to the stack.

#block.is_full() # Returns whether the stack is full.

#block.is_empty() # Returns whether the stack is empty.

